services:
    database:
        image: postgres:15-alpine
        ports:
            - "5432:5432"
        environment:
            - "POSTGRES_USER=user"
            - "POSTGRES_PASSWORD=iamgroot"
#        volumes:
#            - ./data:/var/lib/postgresql/data