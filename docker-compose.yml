services:
    db:
        image: postgres:latest
        ports:
            - "5432:5432"
        environment:
            - "POSTGRES_USER=${DB_USER}"
            - "POSTGRES_PASSWORD=${DB_PASSWORD}"
